import axisGridHelper from 'pancake-factory/utils/axis-grid-helper';
import { module, test } from 'qunit';

module('Unit | Utility | axis-grid-helper', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = axisGridHelper();
    assert.ok(result);
  });
});
